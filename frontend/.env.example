# Copy this to .env and fill in values

# Required — URL of the running backend API
VITE_BACKEND_URL=http://localhost:8000

# Optional — if the backend is started with API_KEY set, provide the same value here
VITE_API_KEY=your_api_key_here
